

var ClozeCard = function(text, cloze) {
    this.cloze = cloze;
    this.partial = partial;
    this.fullText = cloze + partial;
    this.createCard = function(err, data){
        if (!fulltext) {
            console.log(err);
        }

    }
};

module.exports = ClozeCard;